/**
 * @param {number[]} nums
 * @return {number[]}
 */
var findDisappearedNumbers = function(nums) {
    let m = new Map();
    let res = [];
    for (let ch of nums)
        m.set(ch, (m.get(ch) || 0) + 1);
    for (let i = 1; i <= nums.length; i++)
    {
        if (!m.has(i))
            res.push(i);
    }
    return res;
    
};