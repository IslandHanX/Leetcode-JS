/**
 * @param {number[]} nums
 * @return {number}
 */
var majorityElement = function(nums) {
    let cand = null, count = 0;
    for (const x of nums)
    {
        if (count === 0)
        {
            cand = x;
            count = 1;
        }
        else if (x === cand)
            count++;
        else
            count--;
    }
    return cand;
};
// Boyer–Moore Majority Vote 算法
// 遇到与候选相同则 cnt++，不同则 cnt--；
// 因为多数元素出现次数 > ⌊n/2⌋，最终候选必然是它。
// count 是净领先票数