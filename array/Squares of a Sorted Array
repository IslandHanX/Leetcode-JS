/**
 * @param {number[]} nums
 * @return {number[]}
 */
var sortedSquares = function(nums) {
    let tmp = [], ans = [];
    for (let i = 0; i < nums.length; i++)
    {
        let c = nums[i] * nums[i];
        tmp.push(c);
    }
    ans = tmp.sort((a, b) => a - b);
    return ans;
};