/**
 * @param {number[]} nums
 * @return {number}
 */
var missingNumber = function(nums) {
    let n = nums.length;
    let x = 0;
    for (let i = 0; i < n; i++)
        x ^= i ^= nums[i];
    return x;
};
// 还是异或