/**
 * @param {string} s
 * @param {string} t
 * @return {character}
 */
var findTheDifference = function(s, t) {
    let x = 0;
    for (const ch of s)
        x ^= ch.charCodeAt(0);
    for (const ch of t)
        x ^= ch.charCodeAt(0);
    return String.fromCharCode(x);
};
