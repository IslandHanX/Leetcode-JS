/**
 * @param {number[]} nums1
 * @param {number[]} nums2
 * @return {number[]}
 */
var intersection = function(nums1, nums2) {
    const n1 = new Set(nums1);
    const res = new Set();
    for (const x of nums2)
    {
        if (n1.has(x))
            res.add(x);
    }
    return [...res];
};